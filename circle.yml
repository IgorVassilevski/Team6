machine:
  java:
    version: openjdk8

dependencies:
  pre:
  - sudo add-apt-repository ppa:cwchien/gradle -y
  - sudo apt-get update
  - sudo apt-get install gradle-2.13

test:
  override:
    - gradle test -Dtests.jvms=1
    